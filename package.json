{
  "name": "codex-image-gen",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw plugin for Codex CLI image generation ($imagegen).",
  "main": "index.ts",
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
